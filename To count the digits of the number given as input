#include <stdio.h>
int main() {
    int num,n1,d;
    int sum=0;
    printf("enter value for num: ");
    scanf("%d", &num);
    n1=num;
    while(num!=0){
         d=num%10;
         sum=sum+d;
         num/=10;
    }
    printf("sum of the digits is %d", sum);

    return 0;
}
